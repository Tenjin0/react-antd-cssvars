import React from "react"

import { Theme, TThemeColorTypes } from "."
describe("Test Theme Component", () => {
	test("displays a user after a click", () => {
		document.body.innerHTML = `
        <body>
            <p class="test">test content</p>
        </body>
   `
	})
})
